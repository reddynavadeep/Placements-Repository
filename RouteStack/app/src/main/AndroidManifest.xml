<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.routestack">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentViewUploads"></activity>
        <activity android:name=".CompanyFireBase" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".Forgot" />
        <activity android:name=".Register" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".ViewUploadsActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".StatsIntent" />
        <activity android:name=".CompanyIntent" />
        <activity android:name=".MainActivity">

            <!-- android:theme="@style/AppTheme.NoActionBar"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>